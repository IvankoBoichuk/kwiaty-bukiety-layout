<section class="bg-[#E5EFDE] py-12">
	{% embed "../layouts/container.twig" %}
		{% block content %}
            <h2 class="text-h2 text-green-default mb-6">{{ fields.title }}</h2>
            <div class="grid grid-cols-2 gap-x-2.75 gap-y-6 auto-rows-auto sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5">
                {% if fields.list %}
                    {% for item in fields.list %}
                        {% include "../tease/product-3.twig" with { item: item } %}
                    {% endfor %}
                {% endif %}
            </div>
		{% endblock %}
	{% endembed %}
</section>
